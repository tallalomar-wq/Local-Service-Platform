{
  "name": "local-service-platform",
  "version": "1.0.0",
  "description": "Local Service Booking Platform - Connect customers with service providers",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["service", "booking", "marketplace"],
  "author": "Tallal Omar",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
